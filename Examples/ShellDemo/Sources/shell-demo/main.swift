// `shell-demo` — example executable exercising the shell operations through the
// `OperationsCLI` command-tree driver.
//
// Scaffold placeholder: the real command wiring (assembling the shell
// operations into an ArgumentParser command tree and dispatching) lands in the
// ShellDemo task. For now this keeps the executable target compiling.
print("shell-demo: scaffold — command wiring lands in the ShellDemo task")
